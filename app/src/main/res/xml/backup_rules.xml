<?xml version="1.0" encoding="utf-8"?>
<!--
  Sprint 19 privacy fix: RepLog's Privacy Policy states data "stays on your device"
  and "we cannot see it". Cloud auto-backup (Android <= 11 full-backup) would copy
  the workout database and preferences to the user's Google cloud, contradicting
  that. We therefore EXCLUDE all app data from cloud auto-backup. Users move their
  data explicitly via the in-app Export / Backup features.
-->
<full-backup-content>
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
