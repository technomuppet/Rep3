.gradle/
.idea/
build/
local.properties
*.iml
captures/
.externalNativeBuild/
.cxx/
app/build/


# Backup file artifacts from previous Android SVG normaliser iterations
*.bak
*.debug.bak

# Stray git-status dump accidentally committed during a previous merge
tatus

# Layered anatomy SVG library (removed in the dead-code PR; do not re-introduce)
app/src/main/assets/anatomy/
# Anatomy tooling (Python script used to centre per-muscle SVGs; superseded by managed registry)
tools/
