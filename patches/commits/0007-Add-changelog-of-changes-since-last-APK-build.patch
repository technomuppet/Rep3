From 7a7f8d1177d71cfdf978b45f70be6d0768d8dec4 Mon Sep 17 00:00:00 2001
From: RepLog Dev <dev@replog.local>
Date: Tue, 23 Jun 2026 10:08:05 +0000
Subject: [PATCH 07/15] Add changelog of changes since last APK build

---
 CHANGELOG_SINCE_APK.md | 118 +++++++++++++++++++++++++++++++++++++++++
 1 file changed, 118 insertions(+)
 create mode 100644 CHANGELOG_SINCE_APK.md

diff --git a/CHANGELOG_SINCE_APK.md b/CHANGELOG_SINCE_APK.md
new file mode 100644
index 0000000..b08763b
--- /dev/null
+++ b/CHANGELOG_SINCE_APK.md
@@ -0,0 +1,118 @@
+# RepLog — Changes since the last APK build
+
+**Base (your building APK):** `79debc5` — *Remove duplicate ExperimentalLayoutApi import*
+**Head:** `eb48fee` — *Volume Landmarks (#12)*
+**Branch:** `sync/apply-improvements`
+**Scope:** 26 files changed · +11,632 / −221 lines · 6 improvement commits
+
+These changes are **additive** and layered on top of your APK-building `main`. None of
+your build-stabilising edits were reverted (CI workflow, `gradlew_fixed`,
+`CoachContracts`, `SettingsViewModel`/`RestTimerService` fixes, the
+`TrainingDnaRepository.kt` filename, removed unit tests).
+
+> ⚠️ **One schema change vs your APK:** the Room DB is bumped **v11 → v12**
+> (adds `WorkoutSession.sessionRating` via `MIGRATION_11_12`). Existing installs
+> will run that migration on update — worth an on-device upgrade check.
+>
+> ⚠️ **Not built here:** there is no Android SDK in the dev environment, so all
+> verification was pure-Kotlin compile (kotlinc 1.9.22) + logic tests + static
+> wiring checks. Run a full `:app:assembleDebug` before release.
+
+---
+
+## Commits
+
+| Commit | Summary |
+|--------|---------|
+| `c7bc1e5` | Sync: 516-exercise library + 27 templates + personalized onboarding |
+| `0157fce` | Priority 2 UX: reps quick-add, RPE/Tempo dropdowns, Home dashboard |
+| `2454cf4` | Priority 3: progression forecasts, recovery dashboard, adaptive templates |
+| `6e68bc9` | Muscle Gap Analysis with one-tap Add to template (#8) |
+| `961e9b5` | Session Rating (#10): post-workout "How was this workout?" |
+| `eb48fee` | Volume Landmarks (#12): weekly sets per muscle vs optimal range |
+
+---
+
+## Roadmap coverage (all 14 items)
+
+| # | Feature | Where |
+|---|---------|-------|
+| 1 | 516 tagged exercises | `assets/exercises.json`, `tools/generate_exercises.py` |
+| 2 | 27 built-in templates | `domain/templates/BuiltInTemplates.kt`, `util/DataSeeder.kt` |
+| 3 | Home dashboard (≠ Workout) | `ui/home/*`, `domain/home/HomeDashboardStats.kt` |
+| 4 | Onboarding personalization | `ui/onboarding/*`, `domain/templates/ProgramGenerator.kt` |
+| 5 | Reps +1/+2/+5 | `ui/workout/ActiveWorkoutScreen.kt` |
+| 6 | Tempo dropdown | `domain/logging/SetInputPresets.kt` |
+| 7 | RPE dropdown | `domain/logging/SetInputPresets.kt` |
+| 8 | Muscle gap + add to template | `domain/musclegap/MuscleGapAnalyzer.kt`, `ui/trainingdna/*` |
+| 9 | Offline workout builder | `domain/templates/ProgramGenerator.kt` |
+| 10 | Session rating | `data/model/WorkoutSession.kt`, `AppDatabase.kt`, `ui/workout/*` |
+| 11 | Progression forecast | `domain/forecast/ProgressionForecaster.kt` |
+| 12 | Volume landmarks | `domain/volume/VolumeLandmarks.kt` |
+| 13 | Recovery dashboard | `domain/recovery/RecoveryDashboard.kt` |
+| 14 | Adaptive templates | `domain/adaptive/AdaptiveTemplateAdvisor.kt` |
+
+---
+
+## Files created (12)
+
+### Domain logic (pure Kotlin)
+- `app/src/main/java/com/replog/domain/templates/BuiltInTemplates.kt` — 27-template catalog (#2)
+- `app/src/main/java/com/replog/domain/templates/ProgramGenerator.kt` — offline rules engine (#4/#9)
+- `app/src/main/java/com/replog/domain/logging/SetInputPresets.kt` — RPE & Tempo presets (#6/#7)
+- `app/src/main/java/com/replog/domain/home/HomeDashboardStats.kt` — weekly progress + streak (#3)
+- `app/src/main/java/com/replog/domain/musclegap/MuscleGapAnalyzer.kt` — muscle gap suggestions (#8)
+- `app/src/main/java/com/replog/domain/forecast/ProgressionForecaster.kt` — progression forecast (#11)
+- `app/src/main/java/com/replog/domain/recovery/RecoveryDashboard.kt` — recovery dashboard (#13)
+- `app/src/main/java/com/replog/domain/adaptive/AdaptiveTemplateAdvisor.kt` — adaptive swaps (#14)
+- `app/src/main/java/com/replog/domain/volume/VolumeLandmarks.kt` — weekly volume landmarks (#12)
+
+### Tooling (not part of the APK)
+- `tools/generate_exercises.py` — reproducible generator for the exercise library
+
+---
+
+## Files edited (14)
+
+### Data layer / schema
+- `app/src/main/assets/exercises.json` — 32 → **516 exercises** (#1)
+- `app/src/main/java/com/replog/data/db/AppDatabase.kt` — `MIGRATION_11_12`, DB **v11 → v12** (#10)
+- `app/src/main/java/com/replog/data/model/WorkoutSession.kt` — added `sessionRating: Int?` (#10)
+- `app/src/main/java/com/replog/data/repository/WorkoutRepository.kt` — `setSessionRating(...)` (#10)
+- `app/src/main/java/com/replog/util/DataSeeder.kt` — template seeding + program install + missing-exercise upgrade (#2/#4/#9)
+- `app/src/main/java/com/replog/util/PreferencesManager.kt` — training-profile + coach cache keys (#4)
+- `app/src/androidTest/java/com/replog/data/db/AppDatabaseMigrationTest.kt` — `migrate11To12` test (instrumentation only)
+
+### UI layer
+- `app/src/main/java/com/replog/ui/home/HomeScreen.kt` — Home → dashboard (#3)
+- `app/src/main/java/com/replog/ui/home/HomeViewModel.kt` — weekly/streak state (#3)
+- `app/src/main/java/com/replog/ui/onboarding/OnboardingScreen.kt` — personalization questions (#4)
+- `app/src/main/java/com/replog/ui/onboarding/OnboardingViewModel.kt` — generate + install program (#4)
+- `app/src/main/java/com/replog/ui/navigation/NavGraph.kt` — onboarding wiring (#4)
+- `app/src/main/java/com/replog/ui/workout/ActiveWorkoutScreen.kt` — reps quick-add, RPE/tempo chips, session-rating dialog (#5/#6/#7/#10)
+- `app/src/main/java/com/replog/ui/workout/ActiveWorkoutViewModel.kt` — `rateWorkout`, `installAllBuiltInTemplates` (#10)
+- `app/src/main/java/com/replog/ui/trainingdna/TrainingDnaInsightScreen.kt` — DNA meaning, recovery, forecasts, swaps, muscle gap, volume cards (#8/#11/#12/#13/#14)
+- `app/src/main/java/com/replog/ui/trainingdna/TrainingDnaViewModel.kt` — combines data for all DNA features (#8/#11/#12/#13/#14)
+
+---
+
+## How to bring this into your repo
+
+A patch bundle is provided at `patches/replog-improvements.bundle` (a git bundle
+of `79debc5..eb48fee`). From a fresh clone of your repo on your machine:
+
+```bash
+# 1. Fetch the commits from the bundle into your clone
+git fetch /path/to/replog-improvements.bundle sync/apply-improvements
+
+# 2. Merge them onto main (fast-forward, since they sit on top of 79debc5)
+git checkout main
+git merge FETCH_HEAD          # or: git merge sync/apply-improvements
+
+# 3. Build & test, then push
+./gradlew :app:assembleDebug
+git push origin main
+```
+
+Alternatively, a flat patch file `patches/replog-improvements.patch` can be applied with
+`git apply` / `git am` if you prefer not to use the bundle.
-- 
2.47.3

