# RC20.3A — Tech Debt

## Unused Files

- `domain/visual/animation/EquipmentAnchoring.kt` — old averaging wrists single line bar floating. No longer used by new SkeletalRenderer or layered pipeline. Grep shows only defined, not imported elsewhere except possibly old tests. Should be removed.
- `domain/visual/body/BodySegment.kt` — DTO defined but HumanBodyRenderer does not use BodySegment list, uses direct params. File unused.
- `domain/library/MuscleMap.kt` legacy 19 regions vs new `domain/visual/anatomy/MuscleMap.kt` 27 regions — old used only in LegacyMuscleBodyDiagram, new used in vector. Old can be removed after legacy boxes removed.
- `domain/library/ExerciseAnimation.kt` legacy Cartesian Points rubber-banding — only used in legacy fallback path ExerciseAnimation.clip. Could be kept as fallback safety but considered dead for main path.

## Unused Classes

- `domain/visual/animation/EquipmentAnchoring` object — unused.
- `domain/visual/body/BodySegment` data class — unused.
- `domain/visual/animation/Chain` data class inside IKSolver.kt defined but not used (Chain joints, lengths).
- `domain/library/Pose` vs `domain/visual/animation/Pose.kt` (SkeletalPose) — legacy Pose used only in legacy fallback.

## Unused Methods

- `BodyOrientationEngine.resolveBenchAngle` — defined but not used elsewhere (bench angle logic duplicated in orient).
- `EquipmentEngine.resolveBenchFromAngle` — used in LayeredRenderingPipeline? Actually LayeredRenderingPipeline uses FloorRenderer always and supportRenderer, benchFromAngleRenderer if benchAngle !=0 — so resolveBenchFromAngle is used? Check: In LayeredRenderingPipeline, benchFromAngleRenderer is computed if spec.benchAngle !=0 via EquipmentEngine.resolveBenchFromAngle — yes used.
- `Bone.scaleLength` — defined but not used in new commercial renderer (thickness from Anthropometry).
- `Anthropometry.totalHeight()` — defined but not used except for validation, could be used.
- `CommercialMotionLibrary` many specific timelines like `stepUpTimeline`, `bulgarianSplitSquatTimeline` etc are used via getTimelineForExerciseName, but some like `arnoldPressTimeline` etc used. All 55 appear used via mapping, so okay.

## Duplicate Renderers

- Old vs new equipment: Old `EquipmentAnchoring` + line in old `SkeletalRenderer` (removed) vs new 22 renderers — duplicate system, old dead.
- Old vs new motion: `KinematicMovementFamilies` old arbitrary now facade to `CommercialMotionLibrary` — not duplicate now, facade intentional, but file still exists as wrapper, could be considered duplicate layer. Could merge into CommercialMotionLibrary directly and remove KinematicMovementFamilies, but kept for backward compat.

## Dead Code

- `SkeletalRenderStyles` object in SkeletalRenderer with boneStrokeCap — still used? Old used, new commercial uses VolumetricRenderer drawCapsule with Round cap directly, not via this object. So object retained but only partially used (maybe still used for equipment line? Actually OlympicBarbellRenderer uses StrokeCap.Round directly, not via this object). So dead.
- `NEUTRAL_POSE` in ExerciseAnimationView.kt inside legacy fallback — used only in legacy path, but legacy path still exists as fallback, so not dead for fallback.
- `lerp` functions inside ExerciseAnimationView legacy — used only in legacy fallback.

## Old Stick Figure Logic

- `LegacyExerciseAnimationView` inside `ExerciseAnimationView.kt` — old single-line stick figure with drawFigure. Still present as fallback, not driving main.
- `LegacyMuscleBodyDiagram` inside `MuscleBodyDiagram.kt` with REGION_BOXES rectangular boxes — fallback if anatomy empty.
- `domain/library/ExerciseAnimation.kt` Cartesian Points — old stick figure generation, fallback.
- `domain/visual/animation/EquipmentAnchoring.kt` — old stick figure equipment line.

All old stick figure logic still present as fallback safety, not driving main UI. Can be removed after validation passes >85% and commercialReady true for all exercises.

## Unused Movement Families

- `MovementFamily.ALL` list 41 families — all used? Check: `MACHINE_PRESS`, `MACHINE_PULL`, `CABLE_FLY` etc — MovementRegistry getAllFamilies returns 41, but CommercialMotionLibrary getTimelineForFamily handles only subset, others fallback to default benchPress. So some families like `MACHINE_PRESS` may not have specific template, fallback to bench. Could be considered unused or not specifically handled. Should have specific templates for each family.

## Unused Adapters

- `VisualEngineAdapter` still has AnatomyRenderMode LegacyBoxes fallback — mostly not used, but safety.
- No unused adapters otherwise.

## Legacy Compatibility Layers That Can Now Be Removed

- `EquipmentAnchoring.kt` — can be removed, replaced by EquipmentEngine.
- `BodySegment.kt` — unused DTO, can be removed or used.
- `domain/library/MuscleMap.kt` legacy — can be removed after LegacyMuscleBodyDiagram removed.
- `domain/library/ExerciseAnimation.kt` legacy Cartesian — can be removed after legacy fallback removed.
- `SkeletalRenderStyles` object — can be removed or merged.
- `KinematicMovementFamilies` facade — could be removed and replace all usages with CommercialMotionLibrary directly, but kept for backward compat for now.

## Tech Debt Summary

- **High:** TopBar/bottomBar per frame extra FK solves performance regression, Column recomposition.
- **Medium:** Duplicate equipment anchoring old, duplicate muscle maps, BodySegment unused, Path allocations torso.
- **Low:** Unused Chain data class, resolveBenchAngle, scaleLength, totalHeight, SkeletalRenderStyles.

## Recommendations

- Remove old EquipmentAnchoring.kt after confirming no tests depend.
- Remove BodySegment.kt or integrate into HumanBodyRenderer.
- Remove legacy MuscleMap after legacy boxes removed.
- Keep legacy ExerciseAnimation as fallback safety until commercialReady 100%, then remove.
- Fix performance high debt first (cache top/bottom, pre-bake, isolate Canvas).

