# RC20.3A — Next Actions

## Critical Fixes Before Production (Must)

1. **Cache topBar/bottomBar** — In ExerciseAnimationView, compute topBar/bottomBar once per timeline via `remember(timeline) { evaluate at 0 and duration*0.5 }` not per frame. Saves 76 objects/frame, easy 5 lines fix.

2. **Isolate Canvas recomposition** — Move elapsedSeconds State into separate composable `CommercialBodyCanvas` that only recomposes Canvas, not Column+Row controls. Currently Column recomposes each frame.

3. **Pre-bake timeline lookup** — Instead of evaluating timeline + interpolation + FK each frame, pre-bake 60fps*duration FloatArray of joint rotations. Then per frame lookup index, no map allocation. Would bring 38 objects -> 0.

4. **Fix Dip Foot Locking** — Dip exercise should have footLock false (feet off ground free hanging like pull-up), currently footLock true for standing causes feet planted incorrectly. In HybridSolver targets, check familyId DIP should allow feet free.

5. **Fix Hip Thrust Orientation** — Hip thrust currently returns STANDING support, should be SUPINE or specific. In ExerciseVisualResolver resolveSupportAndAngle, add case for hip thrust -> SUPPORT SUPINE or SEATED? Actually hip thrust shoulders on bench supine pelvis up, should be supine or seated? Resolver should return SUPPORT? Currently returns STANDING, so orientation standing not supine — bug scored 4/10. Fix resolver.

6. **Fix Cable Row Pulley Anchor** — Currently high 5% for all cable, should be low/mid for row. In EquipmentRenderers CableHandleRenderer pulley anchor based on supportType SEATED_FLAT -> low 95% else high 5% — but for cable row supportType is SEATED_FLAT? Check resolver: cable row returns SEATED_FLAT? In resolveSupportAndAngle, CABLE_ROW returns SEATED_FLAT? Actually for CABLE_ROW family returns SEATED_FLAT 85deg, so pulley low 95% correct. Need verify.

7. **Remove Old EquipmentAnchoring.kt** — After confirming no tests depend, delete file.

## Important Improvements (Should)

8. **Side View Culling** — For sagittal movements squat/deadlift/RDL, front view shows both left/right limbs overlapping. Implement side view mode where only right side rendered (e.g., draw only right shoulder/elbow/hip/knee). Add view param in ExerciseVisualSpec.

9. **Muscle Activation Overlay Synchronized** — Currently muscle diagram separate composable not synced with animation. Add overlay in layered pipeline that highlights primary muscles with alpha pulsing based on contraction (e.g., biceps alpha 0.88 at top of curl).

10. **Bar Path Validation in CI** — Run BarPathEngine.validateBarPath with sampled positions over full cycle in unit test, assert vertical xVar <0.06 for bench etc.

11. **Tempo Pause at Bottom/Top** — Current timeline uses linear keyframes top->bottom->top with no pause. Real lifting has pause at bottom/top. Add EASE_IN_OUT_CUBIC and hold keyframes.

12. **Path Pooling** — Pool Path objects for torso to avoid 2 allocations per frame.

## Nice to Have (Could)

13. **Body Type Variants** — Female, athletic, youthful via Anthropometry factors and BodyType enum.

14. **Gender Switch** — UI toggle male/female silhouettes.

15. **Coaching Overlay Text** — Show "Retract scapula", "Brace core" messages from StabilisationEngine in UI Row.

16. **Pre-bake for All Families** — Build-time script that generates JSON lookup tables for each family.

## Removal Plan for Legacy

- After validation passes >85% and commercialReady true for all 25 representative exercises (current avg 7.2, need 8+), remove:
  - `EquipmentAnchoring.kt`
  - `BodySegment.kt`
  - `domain/library/MuscleMap.kt` legacy
  - `LegacyExerciseAnimationView` and `LegacyMuscleBodyDiagram` fallbacks (keep adapter fallback to generic commercial not legacy clip)
  - `SkeletalRenderStyles` unused
  - `Chain` data class

- Keep `ExerciseAnimation.kt` legacy Cartesian as last resort safety for unmapped exercises until GENERIC_UNMAPPED 0%.

## Production Readiness Checklist

- [x] Commercial body renderer volumetric exists and integrated
- [x] Equipment engine 22 renderers exists and integrated
- [x] Biomechanical joint model realistic limits exists and used in FK clamp
- [x] Hybrid FK/IK foot locking + hand targets exists and used
- [x] COM calculator exists and used in overlay
- [x] Bar path engine 8 types exists and used for hand targets
- [x] Stabilisation engine 6 cues exists and evaluated
- [x] Commercial motion library 55 templates exists and used via VisualEngineAdapter exercise-specific
- [x] Validation suites exist (Rendering + ExerciseMotion)
- [ ] Build succeeds in CI with network (currently fails offline plugin resolution but not code error) — needs Android Studio build confirmation
- [ ] Performance 60fps solid on mid-range (needs cache fix)
- [ ] Exercise scorecard average >8/10 (currently 7.2)
- [ ] No critical bugs: hip thrust orientation, dip foot lock need fixing

## Timeline for RC20.3B

- Week 1: Fix critical 1-7, rerun validation, scorecard target 8+.
- Week 2: Implement should 8-12, performance pre-bake, side view culling.
- Week 3: Final production build verification in Android Studio, generate signed AAB, store listing.

## Honest Next Step

Do NOT add new features (no new equipment, no new body types) until critical fixes 1-7 are done and build passes in proper Android environment. Focus on verification, integration reality, performance.

